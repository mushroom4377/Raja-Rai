{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Next.js Dev Server",
      "type": "node-terminal",
      "request": "launch",
      "command": "cmd /c npm run dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
